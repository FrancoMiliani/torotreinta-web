*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Primera Fuente: Designer (Títulos) */
@font-face {
    font-family: 'DesignerCustom';
    src: url('../fonts/Designer.otf') format('opentype');
    font-weight: normal;
    font-style: normal;
}

/* Segunda Fuente: Montserrat (Cuerpo y otros textos) */
@font-face {
    font-family: 'MontserratCustom';
    src: url('../fonts/Montserrat-SemiBold.ttf.eot'); /* Para compatibilidad antigua */
    src: url('../fonts/Montserrat-SemiBold.ttf.eot?#iefix') format('embedded-opentype'),
         url('../fonts/Montserrat-SemiBold.ttf.woff') format('woff');
    font-weight: 600;
    font-style: normal;
}

/* 2. ESTILOS GENERALES */
body {
    background-color: #252c3d; /* Azul oscuro */
    color: #f5f5f5;           /* Blanco humo */
    font-family: 'Montserrat', sans-serif;
}

/* 3. HEADER Y LOGO */
header {
    background-color: #d81d38; /* Rojo */
    
}

.logo {
    width: 100px;
    display: block; /* Cambiado de flexbox a block para centrar imagen */
    margin: 0 auto;
}

/* Alineación del Menú */
.section-padre {
    display: flex;             /* ESTO ES LO QUE FALTABA */
    justify-content: space-around;   /* Centra los enlaces horizontalmente */
    padding-top: 15px;
    color: #f5f5f5;
    background-color:#252c3d;
    font-family: 'Montserrat', sans-serif;
      font-size: 15px
}

.menu {
    display: inline-block;
    margin-top: 5px;
    list-style-type: none;
    justify-content: space-around;

   
}

.menu a {
    text-decoration: none;
    color:#f5f5f5;
    text-transform: uppercase;
}

/* 4. TÍTULOS */
h1, .titulos {
    font-family: 'DesignerCustom', sans-serif;
    color:#252c3d;
    background-color: #d81d38;
    text-align: center;
    text-transform: uppercase;
    font-size: 25px
}

.explicacion {
    text-align: justify;
    text-decoration: underline;
}

p {
    color: #f5f5f5;
    line-height: 1.6;
    padding: 0 20px;
}
